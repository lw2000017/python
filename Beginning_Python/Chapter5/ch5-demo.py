#!/usr/bin/python -i

# This is the first file-based example in the book

# If this is run on windows, use "python -i ch5-demo.py" to run it as
# it says in the chapter.

a = 10
b = 20

print ("A added to B is %d" % (a + b))
